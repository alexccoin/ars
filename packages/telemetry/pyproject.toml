[project]
name = "ars-telemetry"
version = "0.1.0"
description = "A.R.S instrumentation — spans and metrics, stdlib only, no vendor SDK"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ars_telemetry"]
